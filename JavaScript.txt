
// Полная запись
number = number + 2; // Значение переменной: 2
number = number - 2; // Значение переменной: 0

// Краткая запись
number += 2; // Значение переменной: 2
number -= 2; // Значение переменной: 0

// Увеличение числа на 1
number++; // Значение переменной: 1

// Уменьшение числа на 1
number--; // Значение переменной: 0





// Условная конструкция
if (условие) {
  Инструкции, которые выполнятся, если условие истинно
} else {
  Инструкции, которые выполнятся, если условие ложно
}





// Цикл for of
for (переменная of коллекция) {
  // Код, который нужно выполнить несколько раз
}if





// Оператор строгого равенства
'a' === 'a'; // Результат: true
'a' === 'b'; // Результат: false
   Оператор строгого неравенства
'a' !== 'a'; // Результат: false
'a' !== 'b'; // Результат: true
   Оператор логическое И
if (article.dataset.category !== filter.value && filter.value !== 'all') {
  article.classList.add('hidden');
}
true && true;   // Результат: true
true && false;  // Результат: false
false && true;  // Результат: false
false && false; // Результат: false   
   Оператор логическое ИЛИ
true || true;   // Результат: true
true || false;  // Результат: true
false || true;  // Результат: true
false || false; // Результат: false




// Выводим в консоль
console.log('Привет от JavaScript!');

// Проверяем есть ли у элемента класс
newElement.classList.contains('класс');

// Создаём новый элемент (абзац)
let newElement = document.createElement('p');

// Меняем текстовое содержимое
newElement.textContent = 'Я новый абзац!';

// Добавляем класс
newElement.classList.add('some-text');

// Добавляем элемент на страницу
parent.append(newElement);

// Очищаем поле ввода
newComment.textContent = commentField.value;

commentField.value = '';

// Убираем с элемента класс
newComment.classList.remove('класс');

// чередование классов
newComment.classList.toggle('класс');

// Применение функции и клика
button.onclick = function() {  
  console.log('Клик!');                 
};

// Находим элемент по классу
document.querySelector('.page');

// Найдёт все абзацы на странице
let elements = document.querySelectorAll('p');

// Отменяет отправку данных
element.preventDefault();

// Пример, добавление в список новых задач
let list = document.querySelector('.todo-list');

let input = document.querySelector('.todo-input');

let form = document.querySelector('.todo-form');

let priority = document.querySelector('.todo-priority');


priority.onclick = function () {

  priority.classList.toggle('is-important');

  if (priority.classList.contains('is-important')) {

    priority.textContent = 'Важная задача';
  
} else {

    priority.textContent = 'Обычная задача';

  }

};


form.onsubmit = function (evt) {

  evt.preventDefault();

  let newTask = document.createElement('li');

  newTask.textContent = input.value;

  if (priority.classList.contains('is-important')) {

    newTask.classList.add('is-important');

} else {
  newTask.classList.remove('is-important');

}
  list.append(newTask);
  input.value = '';

};

// Обращаемся к элементам коллекции по индексу
коллекция[индекс]
console.log(elements[0]); // Выведет первый элемент коллекции
console.log(elements[1]); // Выведет второй элемент коллекции

// Получаем значение data-атрибута
<div data-cat-name="Кекс">
элемент.dataset.имяАтрибутаБезПрефикса // без data-
let element = document.querySelector('div');
console.log(element.dataset.catName); // Выведет: Кекс

// Обработчик событий oninput, получаем текст еще до того, как пользователь его отправит
commentField.oninput = function () {

  console.log(commentField.value);

};


// Свойство length, вычисляем длину строки
let text = 'Я люблю JavaScript';
console.log(text.length); // Выведет: 18

// Свойство disabled, блокируем кнопку
 Блокирует кнопку
button.disabled = true;
 Разблокирует кнопку
button.disabled = false;
if (commentField.value.length > 142) {

    commentForm.classList.add('warning');

     submitButton.disabled = true;
}

// Добавляем ветку else
commentField.oninput = function () {

  charCounter.textContent = commentField.value.length;


  if (commentField.value.length > 142) {

    commentForm.classList.add('warning');

    submitButton.disabled = true;

  } else {

    commentForm.classList.remove('warning');

    submitButton.disabled = false;

  }


};

// Оператор сравнения >
console.log(3 > 2); // Вернёт: true
console.log(1 > 2); // Вернёт: false
console.log(2 > 2); // Вернёт: false

// Чтобы отследить прокрутку, используем обработчик событий onscroll.
window.onscroll = function () {
  console.log('Страница прокручена');
}

// Свойство pageYOffset окна браузера содержит количество пикселей
- Если мы на самом верху страницы
console.log(window.pageYOffset); // Выведет: 0
- Прокрутим страницу на 200px
console.log(window.pageYOffset); // Выведет: 200
- Прокрутит страницу на 100px вправо и на 50px вниз
window.scrollTo(100, 50);

// Обработчик событий onchange срабатывает, когда пользователь выбирает новое значение из выпадающего списка
-Находим выпадающий список
let select = document.querySelector('select');
-Добавляем обработчик событий
select.onchange = function() {
-Выводим в консоль новое значение
  console.log(select.value);
};

// Обработчик событий oninput срабатывает плавно в отличии от onchange

// Задаем элементу цвет

let longread = document.querySelector('.longread')

longread.style.color = 'purple'



